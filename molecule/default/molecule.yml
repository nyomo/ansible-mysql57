---
dependency:
  name: galaxy
  enabled: False
driver:
  name: docker
platforms:
  - name: docker
    image: docker.io/pycontribs/centos:7
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint .
